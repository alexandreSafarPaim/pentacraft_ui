import React from 'react';
import { PCLayoutMenuItemProps } from '../../../types/types';
export declare const PCLayoutMenuItem: ({ children, href, collapseItens, icon, }: PCLayoutMenuItemProps) => React.JSX.Element;
