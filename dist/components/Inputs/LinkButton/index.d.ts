import React from 'react';
import { PCLayoutButtonProps } from '../../../types/types';
export declare const LinkButton: ({ children, onClick, className, style, href, target, download, ...props }: PCLayoutButtonProps) => React.JSX.Element;
